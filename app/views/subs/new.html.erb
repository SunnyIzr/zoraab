<div class="row">
  <h2> Add a New Subscription </h2>
</div>

<div class="row">
  <div class="large-4 columns">
    <%= form_for @sub do |f| %>
      <%= label_tag(:cid, "Chargify ID") %>
      <%= text_field_tag(:cid) %>
      <%= submit_tag("Save to Coz", class: 'button') %>
    <% end %>
  </div>
</div>
